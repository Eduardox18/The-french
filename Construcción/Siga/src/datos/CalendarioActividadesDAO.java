package datos;

//import logica.CalendarioActividades;

/**
 * Interface para la clase CalendarioActividades del proyecto. Se declaran los métodos definidos en el diagrama
 * de clases. Los métodos fueron comentados
 * porque no se utilizarán en la parte funcional a presentar del programa.
 */
public interface CalendarioActividadesDAO {
    
    /**
     * Lista de métodos declarados en el diagrama de clases pero que no se utilizarán en la versión
     * actual del programa.
     */
    //public boolean agregarCalendarioActividades(CalendarioActividades calendarioA);
    //public CalendarioActividades consultarCalendarioActividades(int noCalendario);
}
